<h1>新規投稿画面</h1>
<% @tweet.errors.full_messages.each do |message| %>
  <%= message %>
<% end %>
<%= form_for @tweet do |f| %>
  <div class="form-group">
    <div class="col-lg-6">
      <%= f.text_field :text, placeholder: "ツイート内容" %>
    </div>
  </div>
  <div class="form-group">
    <%= f.submit "送信", class: "btn btn-primary withripple" %>
  </div>
<% end %>
